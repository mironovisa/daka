import { StyleSheet, Platform } from "react-native";

const styles = StyleSheet.create({
    detailTitle: {
    fontWeight: "bold",
    fontSize: 14,
    color: "#F24E61",
  },
});

export default styles;
