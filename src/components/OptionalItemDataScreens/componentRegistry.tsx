// componentRegistry.js

import CarComponent from "./screens/CarComponent";
// Import other components here

const componentRegistry = {
  CarComponent,
  // Add other components to the registry
};

export default componentRegistry;
