import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    titleProducts: {
        justifyContent: 'space-between',
        flexDirection: "row",
        alignItems: "center",
        // backgroundColor: "red",
        // marginRight: 12,
    },
    productsContainer: {
        width: '96%', marginHorizontal: "2%",
    },
    topicText: {
        fontWeight: "bold",
        fontSize: 16,
    },
    showMoreTitle: {
        fontWeight: "bold",
        fontSize: 14,
        color: "#F24E61",
        marginRight: 12,
    }
})

export default styles;