import { View, Text } from "react-native";
import React from "react";

const RedirectionCategoryUtil = (categoryId) => {
  return (
    <View>
    </View>
  );
};

export default RedirectionCategoryUtil;
